project(D-07-nonlinear)
add_executable(${PROJECT_NAME} main.cpp definitions.cpp)
set_common_target_properties(${PROJECT_NAME} "HERMES2D")
IF(EXISTS tests)
  add_subdirectory(tests)
  enable_testing()
ENDIF(EXISTS tests)